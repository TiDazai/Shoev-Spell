name: Tests

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Prepare lexicon
        run: ./scripts/prepare-lexicon.sh
      - name: Run tests
        run: swift test
